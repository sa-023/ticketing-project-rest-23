server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5432/ticketing-app
spring.datasource.username=postgres
spring.datasource.password=admin

spring.jpa.show-sql=false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=create

# · Create entities first: spring.jpa.defer-datasource-initialization=true, then load the data: spring.sql.init.mode=always.
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Keycloak properties.
keycloak.realm=company-dev
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.ssl-required=external
keycloak.resource=ticketing-app
keycloak.credentials.secret=RRFjRddGFpohQvmo2oSCPI4FJ0r5QzU9
keycloak.use-resource-role-mappings=true
keycloak.bearer-only=true
spring.main.allow-circular-references=true


master.user=admin
master.user.password=admin
# · When we create a new user with keycloak through Springboot, it is looking for one client under the master realm.
#   Because of that, we created a master-client under Master Realm in Keycloak and added the below configuration here.
master.realm=master
master.client=master-client